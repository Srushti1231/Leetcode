class Solution:
    def lengthOfLongestSubstring(self, s):
        res = 0
        cur = ""

        for c in s:
            if c in cur:
                cur = cur[cur.index(c) + 1:]
            cur += c
            res = max(res, len(cur))

        return res
